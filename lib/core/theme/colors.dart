import 'package:flutter/material.dart';

class AppColors {
  // Fondos
  static const midnight = Color(0xFF0A0F1E);
  static const surface = Color(0xFF111827);
  static const surfaceLight = Color(0xFF1C2537);
  static const border = Color(0xFF2A3548);

  // Acciones
  static const accent = Color(0xFF00D4AA);
  static const accentDim = Color(0x1A00D4AA);

  // Semáforo
  static const success = Color(0xFF00D4AA);
  static const warning = Color(0xFFF5A623);
  static const danger = Color(0xFFFF4757);

  // Créditos / bloques
  static const bloqueEsenciales = Color(0xFF4A90D9);
  static const bloqueEstilo = Color(0xFFF5A623);
  static const bloqueFuturo = Color(0xFF00D4AA);

  // Texto
  static const textPrimary = Color(0xFFF0F4FF);
  static const textSecondary = Color(0xFF8892A4);
  static const textMuted = Color(0xFF4A5568);

  // Premium
  static const gold = Color(0xFFF5A623);
  static const premium = Color(0xFF6C63FF);
}
